<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="singC.Pages.FileBrowserPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid RowSpacing="8" Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ComboBox
                x:Name="DefaultUrlComboBox"
                Grid.Column="0"
                PlaceholderText="已配置网页"
                SelectionChanged="DefaultUrlComboBox_SelectionChanged"
                HorizontalAlignment="Stretch"/>

            <TextBox
                x:Name="UrlTextBox"
                Grid.Column="1"
                PlaceholderText="输入完整的 HTTP/HTTPS 地址"
                KeyDown="UrlTextBox_KeyDown"/>

            <Button
                Grid.Column="2"
                Content="打开"
                Click="OpenButton_Click"/>

            <Button
                Grid.Column="3"
                Content="刷新"
                Click="RefreshButton_Click"/>
        </Grid>

        <TextBlock x:Name="BrowserStatus" Grid.Row="1" TextWrapping="Wrap"/>
        <WebView2
            x:Name="FileBrowserWebView"
            Grid.Row="2"/>
    </Grid>
</Page>
